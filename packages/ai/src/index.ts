/**
 * `@budget-tracker/ai` — public barrel.
 *
 * Real exports land as follow-up commits in this round: PII stripper,
 * tool adapters, registry, Anthropic client wrapper. This empty
 * bootstrap is so `pnpm --filter @budget-tracker/ai typecheck` runs
 * end-to-end before any business logic exists.
 */

export {};
